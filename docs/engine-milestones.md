# Engine build milestones

This is a living working document, not an archived baseline. Edit it in place as
milestones complete or the plan changes — git history is the record of how this
page looked before, so nothing here needs to be copied into a new file first.
Durable, rarely-changing product design lives in [roadmap.md](roadmap.md) and
[editions/](editions/); this page tracks the specific free-data-first engine build
sequence agreed on 2026-08-24.

## Version history

| Version | Date | Change |
| --- | --- | --- |
| 0.1 | 2026-08-24 | Initial milestone list: local environment verified, engine build sequence agreed (free-data pilot mode first, production mode after). |
| 0.2 | 2026-08-24 | Milestone 2 (mode toggle) and the first slice of Milestone 3 (`regime_filter`) built and verified end-to-end against the live FRED API. |
| 0.3 | 2026-08-24 | `factor_engine` built: real cross-sectional momentum ranking (Yahoo price data, 10y history) plus a real per-symbol MACD/RSI backtest with trade log and Sharpe/win-rate/drawdown metrics. Macro model expanded to 8 factors (added rates, PPI, core PCE, nonfarm payrolls). `engine/`/`pipeline/stages/` reorganized into one-file-per-concept subpackages. |
| 0.4 | 2026-08-24 | Milestone 3 completed: `allocation_engine` (real risk envelope + decision graph) and `instrument_engine` (-5..+5 conviction scale, real Black-Scholes options pricing) built and verified end-to-end. Added the desk-level backtest aggregate (equal-weighted across all backtested staging symbols). Frontend: chart timeframe selector + RSI/MACD panes, full backtest trade ledger table, chart markers clipped to the visible timeframe (fixes a squish bug on short windows), US Eastern timestamp display. Docs restructured: Edition V2 recorded, roadmap gap descriptions corrected to match what's actually real now. |
| 0.5 | 2026-08-24 | Moved the staging position-sizing default ($1M notional, 2% risk-per-position) from a Python constant into a seeded `staging_budget_config` DB row — the same "download the repo, see the same default, edit it in one inspectable place" contract already established for `staging_symbols`. `position_size()` now takes budget/risk-fraction as required parameters instead of reading a module constant. Milestone 4's scope written out in full below, at the user's explicit request, so a future session knows what "working" means at that stage before starting it. |
| 0.6 | 2026-08-24 | Backfilled the `strategies`/`strategy_versions`/`strategy_diagnostics`/`strategy_lifecycle_events` registry (present in schema since Edition V1, never populated) with the 5 real engine algorithms: `macro_regime_composite`, `cross_sectional_momentum`, `macd_rsi_single_name_timing`, `risk_envelope_allocation`, `conviction_instrument_selection`. Each carries its real hand-picked parameters, an honest thesis/expected-edge disclosure, a `code_reference` to its source file, a `naive-v1` version, a new `verification_status` flag (`registered_only` until Milestone 4's gate passes), `next_review_at` (6 months out), and explicit-NULL `decay_rate`/`estimated_capacity_usd` diagnostics — all seeded via `INSERT OR IGNORE` in schema.sql, not Python or markdown, per the standing "everything is a table, a row, a data entry" rule. The existing Strategy registry/detail pages render all of it with no new UI built. |
| 0.7 | 2026-08-24 | Milestone 4, step 1 (statistical validation) built and run for real: `engine/research/` computes real Pearson correlation + a real two-sided p-value (scipy) between every macro factor's period-over-period change and every staging symbol's forward return, corrected for the resulting multiple-comparisons problem (hand-rolled Benjamini-Hochberg, verified against a hand-checked example). Persisted to new `factor_significance_runs`/`factor_significance_results` tables, triggered on demand (`POST /api/v1/admin/research/factor-significance/runs`) against the latest sealed dataset — deliberately not wedged into the Milestone-3 manual pipeline. First real run: only 66 of 176 pairs were testable (`fetch_data`'s FRED window was 400 days, sized for `regime_filter`'s YoY math — too short for the 5 monthly macro series to reach a usable sample size) and found one result later shown to be a small-sample artifact. |
| 0.8 | 2026-08-24 | Extended `fetch_data`'s FRED window from 400 days to 10 years (`FRED_OBSERVATION_WINDOW_DAYS`, matching `PRICE_FETCH_RANGE`) so every macro series has enough history to test. Re-ran fetch/validate/regime/factor/allocation/instrument end-to-end (6,330 real FRED observations, up from 688) and re-ran the significance research: **all 176 of 176 (factor, symbol) pairs were now testable**, and exactly one survived correction — NFCI vs. XLV, r=+0.18, adjusted p=0.0059, n=516. The earlier 0.7 finding (NFCI vs. XLP, r=-0.61 on only 51 samples) is gone with the larger sample — a concrete, in-repo demonstration of why the small-sample result was never trustworthy in the first place, and why this step has to run before any weight gets fit to it. |
| 0.9 | 2026-08-24 | Persisted the -5..+5 `conviction` score (`conviction_from_composite()`) onto `cross_section_rows` and `position_candidates` instead of computing it internally and discarding it — it was driving structure selection this whole time but was never actually visible anywhere in the app. Now shown as a real number (`ConvictionBadge`) on the Today page's Cross-sectional evidence matrix (every ranked symbol, not just ones with a position candidate) and on each Proposed position expression card. |
| 0.10 | 2026-08-24 | Widened `strategy_versions.verification_status` from a 2-state flag to the full honest vocabulary: `registered_only`, `verified`, `not_significant`, `collinear`, `decayed`, `outdated` — none of them a kill switch; a naive-v1 function keeps running (`strategies.status='active'`) no matter what this says, because breaking a working page to enforce a correctness claim staging never made is worse than an honestly-labeled naive suggestion. `list_strategies()` now surfaces `code_reference`, `verification_status`, `next_review_at`, and a real `last_checked_at` (`MAX(as_of)` across that version's diagnostics) on the registry list page, not just the detail page. `run_factor_significance_research()` now auto-writes a `factor_significance_summary` diagnostic onto `macro_regime_composite` every time it runs — a real, auto-updating link from the research layer to the registry, deliberately NOT flipping `verification_status` (this run tests individual factors against individual symbols, not the regime composite as one unit; claiming more would overclaim). Required a local dev-database reset: SQLite can't widen an existing CHECK constraint in place, and `data/desk.db` is gitignored, disposable runtime data — anyone pulling this commit onto a fresh clone gets the wide CHECK from schema.sql with no migration needed. |
| 0.11 | 2026-08-25 | **Macro research 2**: `macro_regime_composite` promoted naive-v1 -> naive-v2 (`backend/engine/regime/scoring_v2.py`). Real markets price the SURPRISE in a macro release against an expectation already priced in, not its level or its distance from an arbitrary fixed target (Andersen, Bollerslev, Diebold & Vega, 2003; Balduzzi, Elton & Green, 2001) -- the same "beat vs. miss" logic CME FedWatch applies to policy-rate expectations (Krueger & Kuttner, 1996). No free real-time consensus/survey feed exists for this project (Trading Economics is the planned paid source), so the disclosed, naive expectation proxy is a trailing statistical mean of the series' own history -- an adaptive-expectations stand-in (Muth, 1961), not a market consensus. Same 8 factors, same weights, same aggregation as naive-v1 -- only the per-factor formula changed, per the "one strategy, one step improve" rule. Verified with a concrete contrast test: a steady 5% YoY inflation trend scores near-neutral (no real surprise), while an identical trend with a sudden final-month jump scores meaningfully more negative -- the exact "beat/miss, not level" behavior naive-v1 could not produce. naive-v1's code stays untouched and importable so any dataset snapshot already sealed under it stays honestly reproducible. |
| 0.12 | 2026-08-25 | **Modular swap-safety proof, demonstrated on `cross_sectional_momentum`**: each engine algorithm is a self-contained file (`backend/engine/<layer>/<name>.py`) with its own isolated test file that runs standalone against no DB/network -- a pluggable-strategy architecture (Strategy pattern), not a monolith. This milestone proves the swap contract holds: `momentum_v2.py` was written and fully tested in isolation, then wired into `factor_engine.py` as a one-line swap, and the full pipeline plus existing test suite passed unchanged -- replacing one brick did not break the building. The real statistical content (v1's fixed 0.2/0.3/0.5 horizon blend replaced by weights from a real Pearson + Benjamini-Hochberg significance test against pooled forward returns; first live run found 3M/6M significant, 1M not, consistent with Jegadeesh & Titman 1993) is real but secondary here -- a coding-agent-depth pass, not a literature review or research-team-grade validation, which this milestone deliberately does not claim. v1's code stays untouched and importable; equal-weight fallback keeps the score visible even when nothing clears correction, per "naive over broken." The intent: once a separate research effort (outside this coding pass) finds a genuinely better parameter, decay estimate, or capacity figure for any one brick, it can be swapped in and backtested the same isolated way -- letting research, operations, and trading-desk work proceed independently without one breaking another. |
| 0.52 | 2026-08-27 | **H-MACRO02: balance-sheet predictors -- a real methodological fork from H-MACRO01, made deliberately, not by reusing the same shape blindly.** Balance sheet is a continuous stock, not discrete FOMC steps like Rate -- a naive weekly-direction "event" framework would mostly capture routine operational noise (reserve-management bill purchases, technical reinvestment), the exact QE/Liquidity conflation the framework README already named. New `research_lab/balance_sheet_predictors.py`: real, pooled, continuous IC instead -- each indicator's level vs. `WALCL`'s own forward 13-week (~1 quarter) percentage change, Pearson + Benjamini-Hochberg, 4-week stride to control overlap. Real result: 10 of 23 significant (richer than Rate's 4/22) -- rate-level and credit-tightening signals (IORB, SOFR, DFII30, DFEDTARU/L) predict forward contraction; NFCI's stress signal predicts forward expansion, both economically coherent directions. Two credit-spread results (BAMLH0A0HYM2/BAMLC0A0CM) real but entirely confined to the 2023-2025 QT window -- disclosed as possibly regime-specific, not asserted as durable. A concrete, real demonstration that test *shape* matters as much as data availability: the same short-history series that landed "not done" under H-MACRO01's discrete-event framing (only 6 real events) cleared the floor easily under this paper's continuous framing (n=36) -- no "not done" rows this run at all. `macro-research/README.md` updated: Liquidity's real data source not yet confirmed (SRF/discount-window usage has no verified clean FRED series; likely needs the NY Fed's own operation results, a different, unverified source, not assumed available); Guidance named as a genuinely different, NLP-shaped problem needing real speech/statement text this project doesn't have. The human+agent composite explicitly stays queued -- two dimension papers isn't enough to distill from yet, matching the user's own stated sequencing (more hypotheses first, composite after). |
| 0.51 | 2026-08-27 | **H-MACRO01: first real indicator-vs-Rate-decision table, ground truth derived directly from data.** User's exact requested format: one hypothesis-per-.md-file, an indicator-by-indicator results table, and "not done yet: <specific real reason>" for genuine gaps instead of forced conclusions. New `research_lab/rate_decision_predictors.py`: real hike/cut events derived from `DFEDTAR`/`DFEDTARU`/`DFEDTARL` level changes (2004-2026), no hand-curated FOMC calendar -- holds deliberately not classified, disclosed as a real v1 limitation, not silently absorbed. 3 more free FRED series added to production for this ground truth; caught and fixed a real gap before it broke anything -- DFEDTAR's permanent discontinuation (2008-12-15) needed `max_age_days=None` as an explicit "never check freshness" sentinel, threaded through both `validate_data.py` and a second use site in `fetch_data.py` (`data_assets.max_age_seconds`) a first pass missed, found by a live pipeline run raising `TypeError` before it could reach anyone. 168/168 backend tests passing. Real result against all 22 other layer-1 indicators, Pearson + Benjamini-Hochberg: 4 significant (NFCI, VIXCLS, T10YIE, T5YIE -- all market-based/forward-looking; none of the slower macro releases individually predicted hike-vs-cut direction on their own). `BAMLH0A0HYM2`/`BAMLC0A0CM` correctly landed "not done -- only 6 real events in their 3-year window," the exact honest-gap case the requested format exists to surface, not paper over. `macro_regime_composite` untouched -- this is evidence for a candidate future Rate dimension, not a change to the live composite. |
| 0.50 | 2026-08-27 | **14 new free FRED series fetched into production, live-verified -- the macro-research layer-1 input signals, now real data instead of a proposed list.** `SERIES_METADATA` (`common.py`) extended 8 -> 22; `fred_observations` already series-agnostic, zero schema change. Every candidate ID verified live against the real FRED API before being wired in (`fetch_data`'s existing hard-fail-on-empty-series check means one wrong ID would have broken the whole stage, not just skipped a series) -- caught in advance, not discovered by breaking the pipeline. `make_fred_fetcher`'s test fixture (plain dict lookup, no fallback) would have `KeyError`'d on every full-pipeline test the moment production code referenced an untested series_id -- found and fixed before running the suite, not after a red run. 168/168 backend tests passing. Live-verified end to end: a real pipeline run fetched 50,196 real FRED observations across 22 series (was 13,793/8). One real freshness-threshold miss caught and corrected the same way PCEPILFE's was: GDPC1's freshest print was 147 days old against an initial 120-day guess, corrected to 160 from the real observed number, re-verified clean on a second run. One real, disclosed data-quality finding, not silently absorbed: `BAMLH0A0HYM2`/`BAMLC0A0CM` (HY/IG OAS spreads) only have ~3 years of real point-in-time-vintage history under this project's pinned-`realtime_start` fetch, despite their current/non-vintage display reaching back further -- a genuine ALFRED vintage-archive limit on these vendor-licensed series, confirmed directly against the API with a wide `observation_start`, not a fetch bug. `macro_regime_composite` itself untouched -- these 14 are fetched and available, not yet used by the live composite, per the explicit freeze (0.49). |
| 0.49 | 2026-08-27 | **`docs/hypotheses/macro-research/` subfolder + 3-layer framework, user-designed.** Direct pushback on drifting toward infra/fundamentals work: macro/cross-sectional/timing (all free, bar-and-FRED-driven) still have real potential to mine, and the macro layer specifically has the weakest evidence of the three despite being closest to the user's own trading style -- "we even not have any grey zone evidence quantifiable yet." New structure replaces "does X predict QE/hike/risk-on" single-scalar questions with three deliberately separated layers: input signal (recorded without pre-deciding meaning), Fed response (decomposed into independent Rate/Balance sheet/Liquidity/Guidance dimensions, not one hawkish/dovish scalar), market outcome (kept structurally separate -- a cut is not automatically risk-on). Per-indicator research card template (hypothesis, source, frequency, lag, expected vs. observed, regime dependency, false positives, incremental value); the redundancy/incremental-value check reuses `signal_validation.py`'s effective-number-of-bets machinery unchanged, already proven on the 8 macro factors (0.15) and momentum horizons. `warsh-reaction-function.md` moved into the subfolder as the first paper on this structure: its old R0-R6 response ladder replaced by the 4-dimension table, a market-outcome layer added, the existing July 2026 FOMC observation re-tagged with honest `?`s rather than reconstructed. **`macro_regime_composite` explicitly frozen at naive-v2 until this program produces real, quantified evidence** -- a direct, non-negotiable instruction, now also recorded in cross-session memory so it survives a context reset. Backlog named, not built: input-signal research cards, extending the FRED factor list (Fed balance sheet, TGA, 30Y, real GDP, deficit), a debt-ceiling event study, regime-duration, regime-conditional cross-sectional performance. |
| 0.48 | 2026-08-27 | **Two informal claims preregistered, run, and rejected.** User surfaced two non-literature claims: SPY/QQQ opening gap-downs fill within a few days (`gap-down-fill.md`, H-GAPFILL01); SPY recovers within a few days after a >=3% single-day drop (`large-drop-reversion.md`, H-CRASHREV01). Both required a non-negotiable unconditional-baseline comparison (SPY's real upward drift makes a high raw fill/recovery rate alone spurious) -- and both inverted once compared: gap-down fill rate is high in absolute terms (72-93%) but *lower* than baseline (~99.9%) at every spec, both symbols; SPY's close-based post-drop recovery is significantly *slower* than baseline at every window (5/10/20d), while the looser intraday-high check is uninformative (~ceiling either way). Neither contradicts the already-confirmed H-STREV01 -- H-CRASHREV01 was deliberately specified fresh rather than reusing that weaker, differently-shaped evidence. Real data caveat surfaced and disclosed: SPY's free-tier daily `open` rarely differs from the prior close in this source, understating true overnight gaps (doesn't change either result's direction). `thematic-beta-selection-process.md` also cut roughly in half this pass -- user feedback that it overweighted the research relative to what was needed; same substance, no restated justification. |
| 0.47 | 2026-08-26 | **Thematic beta selection process preregistered (H-BETA01) -- a real pivot in research direction, prompted directly by the user, not a new price/volume anomaly.** An independent, code-blind review the user ran in parallel correctly reframed their own trading history: almost entirely thematic-beta riding (long AI while AI won), not single-name stock-picking, with residual alpha near zero but a real, separate question of whether *which beta, and when* is itself a repeatable skill. Checked against this project's own five-layer hierarchy, not asserted: `macro_regime_composite` (which beta) and `cross_sectional_momentum` (which name/theme within it) map onto the user's real style; `macd_rsi_single_name_timing` (single-name technical entry/exit, the layer that absorbed nearly all of this session's recent engineering -- 0.42, 0.44-0.46) does not, and the user has never traded on it. New `docs/hypotheses/thematic-beta-selection-process.md`: split into 6 minimum ingredients per single-ingredient discipline -- 4 real and buildable now from the existing 2004-2026 staging dataset with zero new data (price leadership, leadership duration, breadth confirmation, a leader-specific deterioration signal reusing H-DOW02's validated detector unchanged), 1 named as an honest gap (fundamental/positioning confirmation -- no data provider exists yet, not faked with a price-only proxy), and 1 inherently qualitative and real-time (theme identification itself), tracked via a new R0-R10 theme-lifecycle ladder -- same shape as `warsh-reaction-function.md`'s response ladder, applied to a theme's lifecycle stage instead of a policymaker's action. Falsification stated plainly: if the ladder's own quantitative checkpoints (breadth, duration, deterioration) show no real relationship to which real episodes go on to sustain vs. revert, the process adds nothing over noise -- a real, useful negative finding, not a result to avoid. Three companion papers named, not built (H-BETA02/03/04) -- the fast-accumulating half of this program, testable now against every historical leadership rotation already in the dataset, unlike the slow, real-time-only qualitative ladder itself. Observation log seeded with one real, retrospective, explicitly pre-process entry (AI infrastructure capex, R1), logged honestly as the prior that motivated the paper, not proof the process works. |
| 0.46 | 2026-08-26 | **Priority 2 closed: 12-1 momentum promoted into the live cross-sectional blend as `naive-v3` -- a real registration decision on already-standing evidence, no new research run.** `12m_skip1m` (Jegadeesh & Titman 1993) had sat `status='draft'` since 0.16, with two real, independent checks already in hand: 0.16 found it genuinely diversifying (correlates only 0.08 with 1m, raised effective number of bets 1.74 -> 2.11), 0.26 found it a real, significant forward-return predictor (r=+0.067) in the opposite direction from 1m/3m's own significant reversal finding -- exactly the reason it needs its own horizon rather than folding into the existing calendar buckets. New `backend/engine/factors/momentum_v3.py`: same real per-horizon significance-weighting mechanism as `momentum_v2.py`, generalized to 4 horizons via a per-horizon `skip_days` (0 for 1m/3m/6m, 21 for 12m_skip1m -- the defining feature of the 12-1 specification: the signal point is one month before the decision date, not the latest close, which every other horizon uses). `_pooled_ic_samples` reused unchanged from `momentum_v2.py` rather than duplicated (pure, version-agnostic). v1's and v2's code both stay untouched and importable. `cross_sectional_backtest.py` (the whole-strategy walk-forward backtest, 0.20) repointed at `compute_cross_section_v3` too -- its own docstring claims to test "the real production ranking," which would have silently gone stale otherwise. `factor_engine.py` repointed (import, sort-order map, factor_key tuple, 6 stale "naive-v2"/"1M/3M/6M" strings corrected to "naive-v3"/"1M/3M/6M/12-1"). 7 new tests (`test_factors_engine_v3.py`, mirroring v2's coverage plus one dedicated to the `skip_days` mechanism itself: constructs a series where the signal-point anchor and the latest close disagree sharply, proving the computed value uses the real 12-1 signal point, not a naive latest-close calculation). `schema.sql` gained `cross_sectional_momentum` `naive-v3`: new `strategy_versions` row (`code_reference='backend/engine/factors/momentum_v3.py'`), `current_version` updated, `12m_skip1m` re-registered under `naive-v3` as `active` with a real `code_reference` (naive-v2's own draft row stays untouched, historical record), new diagnostics (`not_computed`, matching every other fresh promotion), one lifecycle event stating plainly this promotion used no new research. 168 of 168 backend tests passing. Live-verified end to end on a fresh real pipeline run: `factor_engine` reports "naive-v3 IC-weighted cross-sectional momentum," and the DB's own `factor_dimensions` rows confirm the 4th horizon really computed -- `momentum_12m_skip1m`, 21,409 real paired samples, r=+0.059, adjusted p<0.001, weight 0.31. Honest note, not a bug: this live run found all 4 horizons significant and positively signed, including 1m/3m (which the sealed research dataset found significantly REVERSAL-shaped) -- expected non-stationarity already disclosed at 0.42, not a regression. |
| 0.45 | 2026-08-26 | **`short_term_reversal_entry` engineered and wired in as `naive-v3` -- 0.44's draft replacement is now real, running code, live-verified on a fresh real pipeline run.** New `backend/engine/timing/backtest_v3.py` (`run_reversal_rsi_backtest_v3`): long-only entry when trailing 5-day return drops below -3% (disclosed, hand-picked, not fit -- the same naive-parameter honesty every other naive-v1 component in this project already carries), exit on RSI(14) >= 70 (`rsi_overbought_exit`, unchanged, carried forward from naive-v2). Same shape as `backtest_v2.py` on purpose (`BacktestResultV3`, same fields; `no_entry_signal_active` honest-failure path reused unchanged when the entry component is retired) -- a real swap-safety proof one level deeper than 0.12's, this time on a sub-strategy component rather than a whole strategy. 5 new tests (`test_backtest_engine_v3.py`), mirroring `test_backtest_engine_v2.py`'s exact coverage shape: insufficient-data rejection, real trades on real pullbacks, retiring either or both components degrades honestly (open positions stay open, or `no_entry_signal_active`) rather than crashing or fabricating a rule. `schema.sql` gained a real `naive-v3` registration for `macd_rsi_single_name_timing`: new `strategy_versions` row (`code_reference='backend/engine/timing/backtest_v3.py'`), `strategies.current_version` updated, `short_term_reversal_entry` promoted `draft` -> `active` with its real `code_reference` (was `NULL`), `rsi_overbought_exit` carried forward `active`, a new `strategy_lifecycle_events` row recording the promotion, new `strategy_diagnostics` rows (`decay_rate`/`estimated_capacity_usd`, both honestly `not_computed`, matching naive-v2's own pattern -- promotion to running code is not the same claim as a validated decay/capacity estimate). `factor_engine.py` repointed at `naive-v3`/`backtest_v3.py` (import, SQL version filter, call site) and 8 stale MACD-referencing strings/comments corrected to describe the new mechanism accurately (module docstring, symbol_signals rationale, all three branches of the timing_signal event derivation, aggregate/fallback messages). 161 of 161 backend tests passing. Live-verified end to end on a fresh real pipeline run against real market data: `factor_engine` reports **"ran a real reversal-entry/RSI-exit backtest for 22 of them (872 entries logged)"** -- real, non-zero trades, the exact inverse of 0.44's proven `no_entry_signal_active` zero-trade state, now firing for the real reason a real entry trigger exists again. `symbol_events`' per-symbol `timing_signal` rows could not be spot-checked against this specific run: `publish_snapshot` remains scaffolded and not connected to any pipeline stage (a pre-existing, unrelated placeholder, `admin_repository.py:1098`, not touched this pass) -- so no new snapshot was published for the read API to serve, and the existing rows still show stale pre-v3 MACD wording from the last snapshot that did publish. The `factor_engine` stage's own real message is the live proof for this milestone; wiring `publish_snapshot` up is separate, already-named future work, not a gap introduced here. |
| 0.44 | 2026-08-26 | **Priority 3 closed: MACD's entry trigger retired, a real candidate replacement registered honestly as draft -- a real "Ship of Theseus" step, one flawed part swapped for a labeled-imperfect one, not a leap to something fully proven.** Checked the current engine architecture before acting, not assumed: `factor_engine.py` gates `active_timing_components` purely on `strategy_components.status` (active/watching), never on `roles_json` -- so a role-scoped "retire only the entry half" edit would silently do nothing; a real `status='retired'` flip was the only change that actually changes behavior. `UPDATE strategy_components SET status='retired' ... WHERE component_key='macd_crossover'` (both a fresh-clone-safe UPDATE and effective on the already-running dev DB with no reset -- no CHECK constraint touched). New `short_term_reversal_entry` component registered alongside it: `status='draft'`, `code_reference=NULL` -- honestly not yet engineered, only researched (H-STREV01/H-STREV02's real, cost-checked evidence cited directly in its schema.sql comment), `verification_status='registered_only'`, deliberately not `'verified'` -- research evidence and a production-wired, regression-tested entry rule are different bars, not conflated. Two new `strategy_lifecycle_events` rows record both the retirement and the registration with real reasons, not silent flips. Live-verified end to end on a fresh real pipeline run: factor_engine reports "0 entries logged" (was thousands) -- the exact, already-proven `no_entry_signal_active` honest-failure path from 0.13, working correctly for a brand-new real reason three milestones later. The already-built per-symbol timing-signal event (0.27) needed zero code changes to correctly explain the new state too. 156 of 156 backend tests passing (lifecycle-event and component counts updated: 12 and 4). |
| 0.43 | 2026-08-26 | **Short-term reversal's real transaction-cost/turnover robustness -- the prerequisite priority 1 identified last round, now closed with a real, cost-sensitive result.** External review's proposal, checked rather than assumed: H-STREV01's confirmed gross IC alone doesn't establish tradability -- short-horizon reversal is specifically documented in the literature as cost-sensitive, often strongest in harder-to-scale names. New H-STREV02 (`short-term-reversal-cost-robustness.md`): a real, tradable weekly walk-forward strategy directly on the confirmed signal (buy the 5 biggest trailing-5-day losers, hold a week), gross vs. net at four disclosed real round-trip cost assumptions (5/10/25/50bps), scaled by *actually measured* turnover each period, not an assumed constant. New `research_lab/short_term_reversal_cost_robustness.py`. Real result, genuinely cost-sensitive, not a clean pass/fail: gross Sharpe 1.15 (410 real weekly periods, +444.2% total return); net Sharpe 0.96 at 5bps, 0.77 at 10bps, 0.21 at 25bps, -0.73 at 50bps -- the edge survives cost levels plausible for this specific universe (major, liquid ETFs plus two mega-caps) but is fully destroyed by 50bps. Mean turnover 148.7% per period, real and high by construction (ranking by "who crashed most this week" reshuffles the book almost completely, unlike momentum's more persistent rankings). Honest caveat disclosed, not glossed over: the walk-forward spine is constrained by the shortest symbol's real history (XLC, 2018), so this covers ~2018-2026, not the full 2004-2026 the dataset otherwise supports -- the same limitation the existing cross-sectional backtest already has. Recorded `concluded-confirmed (cost-sensitive)`, matching the honest labeling external review proposed rather than forcing a binary promote/reject call the evidence doesn't support. Priority 2 (promote 12-1 momentum) and priority 3 (MACD entry decision, now unblocked by this result) remain open, user's call on sequencing. |
| 0.42 | 2026-08-26 | **Momentum sign bug fixed -- the first of four deferred product decisions from 0.41, closed.** `compute_horizon_weights` (`momentum_v2.py`) weighted each horizon by `abs(correlation)` only, discarding its sign -- flagged live since 0.26, left unfixed on purpose until a real product decision was made to fix it. Real fix, not a rewrite: magnitude-based normalization (unchanged, still sums to 1 and stays numerically stable regardless of how many horizons turn out significant) now gets signed by the correlation's own real, corrected-significant direction as a final step -- `weight` can be negative for a significantly reversal-shaped horizon; every other horizon keeps today's naive positive-momentum default, since there's no validated evidence yet to override it. `_horizon_returns_v2`'s weight-total accumulator switched to `abs(weight)` so a negative weight normalizes correctly instead of destabilizing the denominator. `HorizonReturn`/`HorizonWeightResult`'s `weight` field is now documented as signed. New dedicated test (`test_significantly_reversal_shaped_horizon_gets_negative_weight`) constructs real, engineered mean-reverting data and proves both halves of the fix: the horizon gets a real negative weight, AND a symbol with a strong recent gain on that reversal-shaped horizon scores *less* bullish than a flat one, not more -- the exact behavior the bug produced, now inverted. Existing test asserting weights always sum to exactly 1 was itself relying on the old, now-incorrect invariant -- corrected to assert on magnitude, which is what's actually still guaranteed. 156 of 156 backend tests passing. Live-verified, honestly: a fresh real pipeline run right now shows none of the 3 live-blend horizons (1m/3m/6m) currently clearing significance on this exact fetch -- expected, not a problem, since live market data is non-stationary and 0.41's rerun minutes earlier (a very slightly different real pull) did find 1m/3m significant. The fix is real and unit-proven; its effect on the live blend will show up whenever significance next triggers, which the historical record already shows it does. |
| 0.41 | 2026-08-26 | **2004-2026 rerun of the four price/vol hypotheses that had a real script: one strengthened, two replicated cleanly, one weakened -- the robustness check 0.40 named, now done.** Same four `research_lab` scripts, no changes, pointed at the dataset already sealed in 0.38/0.39 (now including 2008). Real, mixed, genuinely informative result, not a uniform outcome: **time-series momentum weakened** (only 2 of 5 horizons remain significant, was 5 of 5 -- some real support for the "bull-decade artifact" explanation). **Dow Theory trend-structure and risk-state both replicated cleanly** at similar magnitude (r=-0.030 vs -0.039; r=-0.044 vs -0.039) -- real evidence those two aren't decade-specific. **Short-term mean reversion strengthened**, the opposite direction of change from the others: 1-week r=-0.057 (was -0.020), and now significant at 2 weeks too (was not) -- the single strongest, most durable result of the whole session. Each paper's own observation log got a new dated entry with the real numbers (never overwriting the prior checkpoint); the main index's Dataset column now shows `2016+, 2004+` for all four. A temporary tracking table (user-requested, explicitly not meant to persist -- `CHANGELOG.md` is the only file in this project that accumulates forever, other docs stay snapshots, `git log` is the history) lived in `docs/hypotheses/README.md` for the duration of this rerun and was deleted once every row read `done`, per that same instruction. Four items intentionally NOT rerun (they were never research_lab scripts to begin with -- production-factor audits via the real admin API): CS 1m/3m (the still-unfixed momentum sign bug), CS 12-1 (draft, pending a promotion decision), MACD entry (pending a retirement decision), RSI/MACD exit (validated, monitor) -- named here so they aren't lost now that the tracking table is gone, deferred as real product decisions, not further research. |
| 0.40 | 2026-08-26 | **Session recap surfaced a real gap: 8 of this session's 11 hypothesis tests ran on the old, pre-2004-extension dataset; only H-VOLSCALE01 (0.39) accidentally landed on the new one.** Not apples-to-apples, caught while synthesizing "what can we really say" across the full run rather than reporting each result in isolation. `docs/hypotheses/README.md`'s index gained a `Dataset` column (`2016+` vs. `2004+`) and a one-line real-finding column per paper, replacing the checkpoint-count column -- a self-serve recap surface, since re-reading eight full papers every time isn't sustainable as the count grows. Named, not yet done: re-running the 8 pre-extension hypotheses against the real 2004-2026 data (now including 2008) is the highest-value next step -- it tests whether this session's dominant, four-times-repeated "disruption beats order" pattern is a durable market characteristic or an artifact of one strong bull decade, using scripts that already exist rather than new factors. Broadening to new candidate factors is deliberately deferred until that robustness check runs. |
| 0.39 | 2026-08-26 | **First real vol-scaling integration test on a base with standing evidence -- honest, inconclusive result, and an attribution check that changed the conclusion.** External review corrected the originally-proposed design directly: layering H-DOW02's risk signal onto `macd_rsi_single_name_timing` (no proven directional edge, 0.29) would make any Sharpe change uninterpretable; the base needs standing evidence. New H-VOLSCALE01 (`vol-scaled-cross-sectional-momentum.md`), preregistered *before* running it: two real parallel walk-forward equity curves over identical data/ranking/parameters as the real, already-validated `cross_sectional_momentum` strategy backtest (0.20) -- constant exposure vs. exposure cut when the selected top-N symbols' swing structure (H-DOW02's exact detector) is broken. New `research_lab/vol_scaled_cross_sectional_momentum.py`, importing the real `compute_cross_section_v2` from `engine/` (the one-way reuse rule doing real work here, not just a stated policy). First real result looked like a clean win against the naive 100%-exposure baseline: total return +117.7% -> +92.4%, Sharpe roughly flat (0.75 -> 0.76), max drawdown -25.9% -> -19.9%. Before reporting that as the finding, added the attribution check external review specifically asked for: a naive *constant* 77.2% exposure (the scaled version's own mean, with zero structure timing) produced +85.4% return, Sharpe 0.75, max drawdown -20.5% -- nearly identical to the timed version. Honest conclusion: most of the apparent improvement is just being less invested on average, not the timing mechanism specifically; the timing version is directionally, marginally better on both metrics at once, but not distinguishably so against only 85 real periods. Recorded as `concluded-inconclusive` (a new, real status added to the vocabulary, distinct from confirmed/rejected) rather than forced into either. The value of this pass wasn't a clean yes/no -- it was building the attribution check *before* trusting the headline number, exactly the discipline external review argued for. |
| 0.38 | 2026-08-26 | **Staging universe now fetched from a fixed 2004-12-01 anchor (GLD's real launch day) instead of a rolling 10-year window -- a real, live-verified production pipeline change, not a research-only one.** User's own framing, direct: comparing gold's regime behavior against an equity index's across real historical stress (2008) needs every symbol aligned to the *same* real calendar window, not each symbol's own longest available history -- a controlled comparison group, dot-com coverage traded away on purpose since GLD didn't exist yet, 2008 kept. New `STAGING_UNIVERSE_START_DATE = "2004-12-01"` (`common.py`), determined by fetching GLD's real full history directly rather than trusting a remembered IPO date -- the empirical anchor and the stated one differed by two weeks. Real, load-bearing discovery made while implementing, not assumed: Yahoo's `range=max` silently degrades `interval=1d` to a coarser real resolution once the requested span is many years -- verified directly, GLD's real full history via `range=max` returned only 262 bars (weekly/monthly resolution in daily's clothing) versus the real 5,467 true daily bars the identical span returned via explicit `period1`/`period2`. `fetch_daily_bars()` gained a `start_date` parameter that switches to the explicit-window request mode; `range_` stays supported for any caller that wants the old relative-window behavior. `FRED_OBSERVATION_WINDOW_DAYS` extended to match (7,950 days) so macro observations and price bars cover the same real window. Live-verified end to end: a real pipeline run now fetches 113,026 real daily bars (was 55,950) and 13,793 real FRED observations (was 6,330); GLD/SPY/QQQ/IGV all correctly align to 2004-12-01 (5,467 bars each), while XLC (real 2018 listing) correctly keeps its own later real start date rather than being forced earlier -- the "aligned unless the symbol's own real history is shorter" behavior working exactly as intended. All 155 backend tests updated and passing (the test price-fetcher fixture gained the same `start_date` parameter the real one did). |
| 0.37 | 2026-08-26 | **`proportion_significance()`: the missing statistical primitive for probability-shaped hypotheses.** `pearson_significance` answers "does factor level correlate with outcome level" -- it can't answer "is P(rebound >= 10%) actually different between two states," a real, distinct question the risk-state target list (0.36's external review, extended by the user directly: skew, tail probability, rebound/crash probability, stop probability, regime-transition probability) needs. New, real, hypothesis-agnostic addition to `backend/engine/research/significance.py` (not research_lab -- reusable across any future hypothesis the same way `pearson_significance`/`benjamini_hochberg` already are): a real Fisher's exact test (scipy) on the 2x2 contingency table between two groups' hit-rates, chosen over a naive two-proportion z-test specifically because it stays valid on small/imbalanced samples where the normal approximation would not. 5 new tests (large real difference, no difference, small-sample exact-test behavior, both validation-error paths) -- 155 of 155 backend tests passing. Not yet used by any hypothesis; the tool now exists for the probability-shaped candidates already named and queued. |
| 0.36 | 2026-08-26 | **Risk-state reframing, validated directly: a rejected return-direction signal confirmed as a real volatility signal.** Prompted by external review (a parallel, code-blind conversation the user ran alongside this session) correctly naming a real, session-wide methodological gap: every price/volume hypothesis this session was tested exclusively as `factor -> E[r_{t+1}]`, never against volatility, tail risk, dispersion, or persistence -- the broader shape institutional systematic research actually uses. New H-DOW02 (`dow-theory-risk-state.md`), explicitly not a revival of the rejected H-DOW01a (`dow-theory-trend-structure.md` stays rejected, unedited) -- a genuinely different claim: does the same structure-state indicator predict forward realized *volatility*, independent of whether it predicts return direction. New `research_lab/dow_theory_risk_state.py`, reusing H-DOW01a's exact swing detector, swapping only the outcome variable. Real result, confirmed: r=-0.039 (adjusted p=0.0001, n=10,219) -- broken structure predicts higher forward volatility (1.24% mean daily stdev vs. 1.17% for intact), a real, if modest, effect. First clean demonstration this session that a signal can fail as a return predictor and still succeed as a risk-state one -- exactly the reframing external review proposed, now checked rather than assumed. Right-sized deliberately: four more analogous sub-hypotheses were proposed (forward drawdown, cross-sectional dispersion, rebound probability, trend-persistence probability) and queued, not built all at once -- same single-ingredient discipline applied to a new question shape, not abandoned for it. If this signal survives further scrutiny, it is a candidate for a risk-state/sizing role (`strategy_components.roles_json`, role-tagging already proven in 0.13), not the alpha/timing ensemble -- a structural distinction this project's own hierarchy was already designed to support. |
| 0.35 | 2026-08-26 | **Chart hover tooltip + volume promoted to its own pane; the "four independent tests" claim checked, and corrected.** Real UI gap, user-caught: the symbol chart already computed and rendered volume, but overlaid at the bottom of the price pane (a translucent strip), not a real, separate panel between price and RSI/MACD; there was no hover tooltip at all (OHLC/volume/turnover only ever readable from a separate ledger table below the chart). `PriceChart.tsx` reordered into four real panes (price, volume, RSI, MACD, was three) and gained a real crosshair-driven tooltip (OHLC, real volume, and close x volume as an honestly-labeled turnover *approximation* -- no shares-outstanding/float data exists in this project for a real float-based figure). Built with `textContent`, not `innerHTML`, even though every value is a formatted number, not user input. `tsc`, 52 frontend tests, and the build all clean; could not visually confirm the rendered result in a browser -- no browser-automation tool is available in this environment, disclosed rather than silently assumed. Separately, and more importantly: the user's own close reading of the chart ("HH/HL repeats non-stop until it breaks") correctly identified that 0.34's Dow Theory test conflated "state at time i" with a proper persistence claim, and suspected "broken structure" might just be a proxy for the same recent-drawdown condition the other three rejections already captured -- exactly the orthogonality question this project's own Grinold-Kahn framing says has to be checked, not assumed, before treating "four independent tests" as strong evidence. Checked, not left standing: new `research_lab/orthogonality_check.py`, real pairwise correlation across all five price/vol signals this session. Result, more nuanced than either the original claim or the suspicion: `low_vol_63d` and `max_return_21d` are r=+0.78 -- genuinely the same bet, not independent (a big single-day spike mechanically inflates the same window's realized vol) -- but `dow_structure_intact` correlates weakly with everything (\|r\| 0.02-0.12), and `ts_momentum_12m` only modestly (0.12-0.18); effective number of bets across all 5 = 3.84. The corrected, honest claim: not four independent tests, and not one test four times either -- roughly four real, mostly-independent signals with one genuinely redundant pair, still landing the same direction. Both affected papers (`max-effect-lottery-demand.md`, `dow-theory-trend-structure.md`) updated with this finding rather than left overstated. |
| 0.34 | 2026-08-26 | **Dow Theory swing-structure trend test rejected, opposite direction -- the fourth-for-fourth confirmation of this session's dominant, unplanned finding.** User's own operationalization, split price-structure from volume-confirmation on request (single-ingredient discipline, matching this project's own established pattern): H-DOW01a tests only the classical Higher-High/Higher-Low intact-uptrend structure, mechanically defined (a real, non-discretionary fractal swing detector -- a bar is a swing high/low if it's the max/min of a symmetric window around it, confirmed only once enough bars have passed, no look-ahead), against real forward return. Volume confirmation (H-DOW01b) deliberately not touched yet -- moot until a base structural claim survives. New `research_lab/dow_theory_trend_structure.py`, using real OHLC (this project already stores full open/high/low/close/volume, not just close -- confirmed before promising the test was buildable). Cited honestly as a practitioner tradition, not a peer-reviewed anomaly like the other four papers this session, alongside the one real academic evaluation that does exist (Brown, Goetzmann & Kumar 1998, "The Dow Theory: William Peter Hamilton's Track Record Reevaluated," Journal of Finance). Real result: r=-0.039 (adjusted p=0.0001, n=10,219) -- mean forward return during an intact HH/HL structure (+1.19%) was *lower* than during a broken LH/LL structure (+1.71%), the opposite of Dow Theory's predicted continuation direction. This is the fourth independently-specified rejection this session (low-volatility anomaly, time-series momentum, MAX effect, now swing structure) landing on the identical directional signature -- orderly/calm/intact conditions underperform, disrupted/broken/volatile conditions outperform. Four unrelated mechanisms converging the same way is no longer plausibly coincidence; it is real, structural evidence about this specific window's dominant character, named plainly as this session's strongest finding -- and, per `docs/developer-letter.md`, kept entirely in `docs/hypotheses/`, not adapted into the staging pipeline regardless of how consistent it looks. |
| 0.33 | 2026-08-26 | **MAX effect (lottery-demand anomaly) rejected -- a third independent test landing on the same directional signature, worth naming as a pattern rather than three unrelated surprises.** New `research_lab/max_effect.py`: trailing 21-day maximum single-day return (inverted), real IC against 21-day forward return. Chosen deliberately as an orthogonal candidate -- mechanically about tail/skewness behavior (Bali, Cakici & Whitelaw 2011, "Maxing Out"), not trend (0.30) or total variance (0.31). Real result, rejected: r=-0.18 (adjusted p<0.0001, n=10,283) -- the calmest-recent-max third of the sample had a mean forward return of +0.58% vs. +2.41% for the most-extreme-max third, the opposite of the predicted lottery-premium-reversal direction. The pattern: this is the third rejection this session (low-volatility anomaly, time-series momentum, now this) with the identical directional signature -- calm/low-extreme underperforms, volatile/extreme-down-then-recovering outperforms. Three independently-specified tests landing the same way is real, structural evidence about this specific window's character (a dip-buying/recovery-dominant regime), stated as a pattern worth naming, not proof, and not something the app quietly adapts to -- the whole point of keeping research separate from the staging pipeline (docs/developer-letter.md, committed this session) is that findings like this stay in `docs/hypotheses/` until deliberately engineered in, however consistent they look. |
| 0.32 | 2026-08-26 | **Short-term mean reversion: the first CONFIRMED (not rejected) atomic factor this session, at exactly the window predicted.** Both of 0.30/0.31's rejections were reversal-shaped as a side effect of testing continuation/low-vol, not as their own direct claim -- reusing that evidence here would be hypothesizing after the results were known (disclosed explicitly in the new paper, not hidden). Instead: a fresh test, at the effect's own proper window (Jegadeesh 1990's original short-term-reversal specification -- trailing ~1 week, not the 1-month calendar buckets used elsewhere), via new `research_lab/short_term_mean_reversion.py`. Real result, and a clean one: 1-week forward window confirmed (r=-0.0204, adjusted p=0.0020, n=26,044, negative as predicted); 2-week forward window not significant (r=+0.0042, p=0.50, n=26,001) -- the effect empirically resolves within about a week and is gone one week later, exactly the "reversal has a time limit, then becomes ineffective" boundary the user's own framing predicted, now located with a real number rather than asserted. Also addressed directly, not deflected: the user's honest question of whether adding transforms/parameters until something "works" is real science or self-deception -- named the real failure mode (Gelman & Loken's "garden of forking paths"), and the three real defenses against it: pre-specify a transform's theoretical justification before looking at results, correct for the full search space if one is run (not just the winner), and treat out-of-sample/walk-forward validation (still Milestone 4, still not built) as the actual tiebreaker between real structure and overfitting. |
| 0.31 | 2026-08-26 | **Low-volatility anomaly run, and a real 12-1 follow-up check on time-series momentum -- user's own methodological point, tested rather than assumed.** Low-volatility anomaly (`research_lab/low_volatility_anomaly.py`): real, against the sealed dataset -- also rejected (r=-0.19, adjusted p<0.0001, n=10,115; calmest third's mean forward return +0.62% vs. +2.73% for the most volatile third), the opposite of the predicted direction. Not an isolated fluke: the same underlying mechanism plausibly explains both this and 0.30's time-series-momentum rejection -- a high-vol reading is usually a recent drawdown, and if drawdowns get bought aggressively in this window's dominant bull market, that alone produces both "down periods reverse up" and "volatile periods rebound hard." Real scope caveat recorded, not glossed over: only raw forward return was tested here, not the more precise risk-adjusted (Sharpe) form the academic literature usually states. Separately, user raised a real, correct methodological point directly: short-term reversal and medium-term continuation are two distinct, separately time-windowed effects in the literature (Jegadeesh 1990 short-term reversal; Jegadeesh & Titman 1993 medium-term continuation via the 12-1 spec) -- so 0.30's uniform-reversal finding might have been an artifact of blending the two together rather than isolating them. Tested, not assumed: added a proper 12-1 (skip-most-recent-month) case to the same script. Result: still rejected (r=-0.028, adjusted p=0.0066, n=9,317) -- smaller in magnitude than the raw 12m reading (r=-0.046, isolating the short-term effect did reduce it) but still real, still significant, still negative. The methodological question was worth asking and checking; it changed the honest magnitude, not the conclusion -- a genuinely different answer than the same isolation technique gave cross_sectional_momentum's own 12-1 component (0.16), which found real continuation once isolated. |
| 0.30 | 2026-08-26 | **First full preregister -> observe -> conclude cycle on a quantitative hypothesis, plus a second candidate preregistered -- widening the "3 category" work into a real literature-review pipeline.** User's framing, direct: production multi-factor desks run 200+ differently-weighted, individually-gated factors packaged under one name; this project's own `strategy_components`/metric-catalog schema already supports that shape (0.13, 0.17), just not populated at that scale yet -- the right next step is proving the preregister-to-weighting pattern with a couple of real candidates, not attempting to enumerate 200 at once. Two real, literature-backed candidates preregistered as `docs/hypotheses/` working papers: time-series momentum (Moskowitz, Ooi & Pedersen 2012) as a timing-layer challenger to MACD, whose entry trigger 0.29 just found has no real edge; low-volatility anomaly (Ang, Hodrick, Xing & Zhang 2006; Frazzini & Pedersen 2014) as a cross-sectional addition orthogonal to momentum. Added `preregistered` as a real status distinct from `observing` (a continuous, statistically-testable hypothesis can get its first real checkpoint immediately, unlike an event-driven one like Warsh that has to wait for a real-world date). New `backend/research_lab/time_series_momentum.py`: real IC test, all 4 horizons, against the sealed dataset, read-only. First run used no stride and returned p<0.0000 everywhere -- caught as likely inflated by overlapping-window autocorrelation (a 12-month window shifted by one day shares 251 of 252 days with the previous one), corrected with stride=5 matching momentum_v2.py's own established convention, and the real result held: **all 4 horizons significant, but every one NEGATIVE -- the opposite of the literature's predicted direction.** Recorded honestly, not softened or discarded: `time-series-momentum.md` moved straight to `concluded-rejected` (on this universe/window), with a stated, plausible, un-proven mechanism (this window contains one of the strongest sustained equity bull markets on record; "buy the dip" being persistently rewarded produces exactly this pattern) and a real, substantive contrast against cross_sectional_momentum's own finding (0.26 found short-term reversal but genuine 12-1 continuation cross-sectionally; this time-series, own-asset version found uniform reversal at every horizon -- "beating peers" and "beating your own past" are real, different questions, not the same one twice). Low-volatility anomaly preregistered, not yet run. |
| 0.29 | 2026-08-26 | **Timing-signal event-study IC test, closing the third and final "3 category" gap -- and a real split result.** macro_regime_composite and cross_sectional_momentum both got real forward-return IC tests this session (0.25, 0.26); macd_rsi_single_name_timing's two components never did. Deliberately NOT the same pairwise-correlation/ENB shape used for the other two families -- MACD and RSI are role-tagged, sequential triggers (0.13), not co-equal weighted factors -- so this is a real event study: at every day across every tradable symbol's full history, a 0/1 event indicator (did MACD cross bullish / is RSI(14) >= 70 today) paired with the real 21-day forward return, tested via the same `pearson_significance` used everywhere else in this file (a binary-vs-continuous Pearson correlation is mathematically a two-sample mean-difference test -- no new statistics needed). New `run_timing_signal_significance_research()`, persisted via the same general `research_runs`/`research_run_metrics` tables and `factor_ic` catalog metric already used for momentum. Live-verified against real data (~51,000 pooled days per component, thousands of real event days): **RSI-overbought is a real, validated exit signal** (r=-0.015, adjusted p=0.0012, 4,583 real event days, mean forward return +1.10% on event days vs. +1.44% otherwise) -- **but MACD bullish crossover shows no real edge as an entry trigger** (r=+0.002, p=0.66, statistically indistinguishable from a random day) over this universe and decade, which includes a dominant bull run that likely swamps a crossover-timing edge either way. A real, honest, mixed result: the exit rule holds up, the entry rule -- the strategy's only registered entry trigger -- does not, yet. Not fixed or replaced this pass; flagged the same way the momentum sign bug was, as a finding for a deliberate decision, not silently patched. |
| 0.28 | 2026-08-26 | **Real bug, user-caught by actually clicking: the "Run pipeline" button was invisible without scrolling.** User asked "what button do I click to fetch today's data" and, after being pointed at Operations -> Run pipeline, reported back "not found" -- correctly, not a mistake on their part. `OperationsOverviewPage.tsx` rendered the full evidence-gated roadmap (five milestones, each with expandable gate detail -- a long report) between the page header and the actual `Dry preflight`/`Run available stages` buttons, so a page titled "Run pipeline" led with a report instead of the action it's named for. Reordered: pipeline panel (the run buttons + stage list) and the latest-run record now render immediately after the top stat grid; `ProductReadinessPanel` (the long roadmap) moved to the bottom, unchanged internally, still fully present -- verified, not simplified away, since it's real supplementary evidence, just not the primary action. `npm test` (52 tests) and `tsc --noEmit` both clean; confirmed live by rebuilding and re-serving. Same category of finding as 0.19's hash-link bug and 0.18's page reorganization -- caught by a real user trying to actually use the page, not by reading the code. |
| 0.27 | 2026-08-26 | **Real per-symbol current timing signal -- the missing third leg connecting regime, cross-sectional standing, and entry/exit timing into one story.** User-identified gap, direct: "so you have regime/name selection... then timing signal of each, like no exit signal, if hold, still hold." Checked rather than assumed: `symbol_signals` (one row per symbol per snapshot) was already real, but only ever populated with cross-sectional momentum standing (`factor_engine.py`'s own comment already disclosed this: "not the same as the MACD/RSI single-name timing backtest below"); the MACD/RSI backtest's *current* state (as of the latest fetched bar) was computed every run but only ever surfaced as historical entry/exit fill events and desk-level aggregate stats -- never as an explicit "is now the right time" fact anywhere. New logic in `factor_engine.py` (no new table, no new field on `BacktestResultV2` -- derived entirely from `backtest.trades`, already returned): one `symbol_events` row per symbol per snapshot (`event_type='timing_signal'`, `event_status='signal_state'`, matching that column's existing vocabulary for a non-executed current-state read), one of exactly three honest states -- holding (open position, no exit trigger since entry), flat (last position closed, no new entry trigger since), or no signal yet (no MACD crossover has fired in this window at all) -- or the existing `no_entry_signal_active` explanation if the entry-trigger component itself is retired. Zero frontend code needed: `symbol_events`' existing generic read/render path (event ledger, chart markers keyed off `event_status`, not a type allowlist) picks up the new type automatically. Live-verified against real data, using the user's own example symbols: IGV and XLE both **really did exit on RSI-overbought** (2026-08-04 and 2026-08-18) and are flat with no new entry signal as of 2026-08-25 -- a real, direct answer to "does it trigger exit signal of IGV? XLE?". Fuller worked example: SMH (this universe's semiconductor proxy; SOXX itself is not in the 22-symbol staging list) reads cross-sectionally bullish (rank 5 of 22, momentum +12.72%) while its timing state is flat (MACD crossed bearish the day before, 2026-08-24) -- a real, coherent "strong sector, wrong entry moment" read spanning all three layers for the first time. Page-layout reordering (regime before position candidates, matching the decision's actual causal order) discussed, not done this pass -- a design change, not more evidence, deliberately left for a separate confirmation. |
| 0.26 | 2026-08-26 | **Momentum horizon forward-return IC test, closing 0.16's explicit gap -- and a real, unfixed sign bug it exposed.** New `run_momentum_significance_research()`: tests each of cross_sectional_momentum's 4 candidate horizons (1m/3m/6m/12m_skip1m) for real Pearson IC and Rank IC against forward returns, reusing the exact pooled-pairing and Benjamini-Hochberg machinery already proven live in `compute_horizon_weights` (`_pooled_ic_samples` generalized with an optional `skip_days` for the 12-1 specification), persisted via the general `research_runs`/`research_run_metrics` tables against the real catalog metrics `factor_ic`/`factor_rank_ic` (both previously empty, unused since 0.15/0.17). Deliberately does not touch `compute_horizon_weights` or `HORIZON_LOOKBACKS` -- this is evidence about a draft component (`12m_skip1m`), not a change to the live blend. Live-verified against real data (~10,000 pooled samples per horizon, all 22 tradable symbols' full history): **1m (r=-0.044) and 3m (r=-0.039) are real, significant short-term REVERSAL, not momentum; 12m_skip1m is real, significant momentum (r=+0.067); 6m is not significant** -- textbook Jegadeesh & Titman (1993) shape, found in this project's own data for the first time. This exposed a real, live, UNFIXED bug: `compute_horizon_weights` weights each horizon by `abs(correlation)` only, discarding sign -- so a stock's positive 1-month return is currently scored bullish by the live cross-sectional ranking when this same test says that direction is empirically bearish (mean-reverting) over this exact universe and history. Flagged to the user, not silently patched: changing live ranking behavior is a deliberate decision, not a side effect of a research run, per this project's standing research/production separation rule. Fix identified but not applied: sign the weight by the correlation's direction, not just its magnitude. |
| 0.25 | 2026-08-25 | **Macro composite's own regime score gets a real forward-return IC test -- the 0.20 follow-up, closed.** Every individual macro factor's forward-return correlation was tested back in 0.7/0.8; the blended composite score itself never was. New `_macro_composite_score_series()` (`research_repository.py`): at each of CPIAUCSL's own real observation dates, truncate all 8 factors' history to what was actually available then and run it through the real live `compute_regime_v2()` -- the same function that drives the live regime label, not a re-derived approximation -- honestly skipping any anchor with too little trailing history for a factor's surprise window (`InsufficientSeriesDataError`, never padded). The resulting real score series is added as a 9th entry into the same `factor_observations` dict `run_factor_significance_research` already builds, so it rides the exact same Pearson/Benjamini-Hochberg correction batch as the 8 raw factors -- no new table, no new endpoint, no separate correction budget. Live-verified against a fresh real pipeline run (6,329 FRED observations, 55,948 daily bars): 102 real composite-score points from 2018-01 to 2026-07, tested against all 22 staging symbols (198 pairs total) -- **none of the 22 composite-vs-symbol correlations survive correction** (raw p-values 0.20-0.99, |r| <= 0.13), while the pre-existing NFCI-vs-XLV finding (r=+0.18, adjusted p=0.007) reproduced unchanged, confirming nothing else broke. An honest null result, not a bug: a naive linear weighted-sum composite can wash out a real sub-signal (NFCI) by averaging it against 7 others with different lead-lag character -- exactly the kind of thing Milestone 4's decorrelation/regime-conditional-interaction work (not yet started) is supposed to address, not evidence the regime approach itself is broken. |
| 0.24 | 2026-08-25 | **`backend/research_lab/` scratch-code convention; hypotheses folder linked from the app nav.** Direct follow-up to 0.23's Markdown-first hypothesis workflow: a matching code-side quarantine for ad-hoc, hypothesis-testing scripts, governed by two rules stated in its own README -- never imported by production code (`main.py`/`engine/`/`pipeline/`), never writes to the database -- which is what makes "endless, messy experiments, hardcoded values, no tests" safe to allow without risking the running pipeline. Reuse is explicitly one-way: a script here may import the real, hypothesis-agnostic utilities in `backend/engine/research/`, never the reverse. Side nav gained a "Research hypotheses" link (`Newspaper` icon, opens in a new tab) pointing straight at the GitHub-rendered `docs/hypotheses/` folder rather than building an in-app Markdown renderer for something GitHub already renders for free (tables, checklists). `docs/README.md`'s document map updated to point at both. |
| 0.23 | 2026-08-25 | **Retracted 0.22's DB registration; hypotheses now start as Markdown working papers.** User-identified, direct: "research itself, having data structure problem... some [checkpoints] are nominal, some are categorical, some are continuous, some have a duration unit, some are true/false... md file a thesis of paper, is the best." Confirmed against the very table 0.22 had just built: `research_observations.signal_direction` was a `hawkish`/`dovish`/`neutral`/`inconclusive` `CHECK` constraint -- a shape specific to a Fed-policy hypothesis that would not fit, say, a price-action hypothesis at all. Committing to one SQL schema before a hypothesis reaches a real conclusion is premature structure, the same category of mistake as the retracted Edition V3 one level up (engine-milestones.md's own preceding entries) -- undone the same way: retract, don't leave unused. Dropped `research_observations` (table, index, API endpoint, request model, UI panel) and the `warsh_reaction_function` `strategies`/`strategy_versions`/`strategy_components`/`strategy_lifecycle_events`/`research_observations` seed rows from schema.sql -- all net-new in the same Unreleased window, never in a prior release, so a clean retraction rather than a deprecation. New `docs/hypotheses/` folder: a hypothesis now starts as a versioned Markdown working paper (thesis, prior, checkpoint definition, promotion criteria, an inline observation-log table) instead of DB rows; only once it reaches a real conclusion (roughly 10-20 real checkpoints, for a Fed-reaction-function-shaped hypothesis) does it get engineered into a `strategies` row and real SQL, designed from the evidence actually in hand rather than guessed at registration time. The reusable stats utilities from 0.15 (`backend/engine/research/significance.py`, `signal_validation.py`) are explicitly still fair game to call from an ad-hoc script during the paper stage -- they're hypothesis-agnostic, not Fed-shaped, so nothing about them needed to move. First hypothesis under the new pattern: `docs/hypotheses/warsh-reaction-function.md` (H-W01), same real content 0.22 drafted (two independent sub-functions, the 5-group observation taxonomy, the R0-R6 response ladder, speech text-factor keywords, one real July-2026-FOMC observation), now as a working paper. Backend/frontend tests updated for the reverted seed counts (150 passing) and `tsc --noEmit` clean; the DB registration never reached a live-verified end state beyond a test write that was itself immediately removed, so no data migration was needed. |
| 0.22 | 2026-08-25 | *(Retracted by 0.23 the same day -- kept for history, not current.)* Cold-start research infrastructure: `research_observations`, and the first entity registered on it -- a Warsh Fed reaction-function hypothesis. New table, distinct from `research_runs` (requires a sealed dataset, produces computed metrics) and `strategy_components` (a computed-or-overridden numeric value): a real, dated, append-only log of qualitative event checkpoints for a hypothesis with essentially zero sample size -- one row per real-world event, honestly categorized (`hawkish`/`dovish`/`neutral`/`inconclusive`), never a fabricated probability. `POST /api/v1/admin/research/observations` (operator-guarded) and read via the existing `get_strategy()` response; live-verified end to end (a test write, confirmed, then removed). Registered `warsh_reaction_function` (`family='policy_reaction_function'`, `status='draft'`, explicitly not wired into any pipeline stage, per direct request -- "not make it into pipeline yet, I need observation") with a real hypothesis (H-W01: Fed Chair Kevin Warsh reacts primarily to Treasury/credit-market *functioning*, not the absolute yield level) translated in full from the user's Chinese-language research reasoning into precise English for every persisted field, per this project's standing rule that docs/code/DB content stay in proper English regardless of the language a research discussion happens in. Two independent sub-functions registered as `strategy_components` (`monetary_policy_function`, `market_functioning_function`) rather than one blended reaction -- confirmed directly against the existing `macro_regime_composite`: that composite's 8 hand-picked factors (core PCE at weight 0.15) are themselves an implicit reaction-function model tuned for a well-observed chair, and there is no reason to assume those weights transfer to a chair with ~3 months of tenure, so this registers a genuinely separate hypothesis rather than editing that assumption into the existing composite. One real observation seeded (July 2026 FOMC: 9-3 hold, 3 dissenters wanted a hike despite inflation above target, no easing guidance despite elevated yields -- hawkish). The full 5-group observation taxonomy (price, Treasury functioning, funding plumbing, credit transmission, macro), the R0-R6 graduated response ladder, and speech text-factor keyword groups are recorded in the version's `parameters_json` rather than as dedicated columns -- appropriately lightweight for one real datapoint; can be normalized into structured columns later once enough real checkpoints justify it. Next real checkpoint: Jackson Hole keynote, 2026-08-28 10:00 ET (Fed-confirmed). |
| 0.21 | 2026-08-25 | **Display-name correction: "MACD/RSI single-name timing" -> "Single-name timing."** User-identified: a strategy's user-facing name tied to today's specific components reads as wrong the moment one gets retired or a new one is registered -- exactly the confusion the component-registry work exists to prevent. `strategy_key` (`macd_rsi_single_name_timing`) stays untouched, matching the separation this project already applies to `security_id` vs. ticker symbol -- only `strategies.name` and the Methodology page's label changed. Fixed with an `UPDATE ... WHERE name != 'Single-name timing'` alongside the seed `INSERT OR IGNORE` (a fresh clone gets it from the seed; an existing database gets it from the UPDATE, same "correction, not a fresh row" pattern already used for prior fixes) -- live-verified without a database reset. Confirms the underlying question directly: `strategy_key`, the component registry (`macd_crossover`, `rsi_overbought_exit`, both still `active`), and the display name are fully independent -- renaming one touches none of the others. |
| 0.20 | 2026-08-25 | **The "strategy" granularity tier's first real content: a whole-strategy walk-forward backtest.** New `backend/engine/factors/cross_sectional_backtest.py` (`run_cross_sectional_momentum_backtest`, 4 tests): at each rebalance date (naive-v1, hand-picked `top_n=5`/`rebalance_days=21`, not fit), ranks the universe with the real production ranking (`momentum_v2.compute_cross_section_v2`, recomputed from only the history available at that date -- point-in-time by construction), buys the top N equal-weighted, holds to the next rebalance, chains the real returns into an equity curve against a real equal-weight-universe benchmark. Confirms the user's earlier point directly: macro has no strategy-level tier (a classifier isn't traded), cross_sectional_momentum does (its ranking is directly tradable) -- `STRATEGY_BACKTEST_FAMILIES` in research_repository.py names this explicitly rather than leaving it implicit. `POST /api/v1/admin/research/strategy-backtest/runs` live-verified against real data: 85 real rebalance periods (2017-08-25 to 2024-10-01), naive momentum **beats** its benchmark (total return +338.0% vs. +196.0%, CAGR +23.2%, Sharpe 1.22, max drawdown -24.5%, turnover 74%/rebalance) -- a genuinely different, honest character from the single-name MACD/RSI aggregate, which loses to buy-and-hold. Not a validated result: no transaction costs, no out-of-sample split, one hand-picked (top_n, rebalance_days) pair, Milestone 4's gate has not run against it -- stated plainly, not implied otherwise. DB write and GET read-back both independently verified; the metric catalog's `has_data` now shows real content at the `strategy` level for the first time. Also named, not built: the user separately identified a real, distinct gap -- nothing tests whether the macro composite's own weighted regime score (as opposed to each of its 8 factors individually, already tested) predicts a real forward outcome; queued as a follow-up, not attempted here so as not to derail the agreed next step. |
| 0.19 | 2026-08-25 | **Two real bugs from 0.18, both user-caught by actually clicking the link.** (1) `/operations/research#cross-sectional-momentum` didn't scroll anywhere -- React Router does not replicate a browser's native behavior of jumping to a URL's `#id` on client-side navigation (only a real page load does that), so the user landed on the macro section at the top of the page while the URL quietly said momentum, and reasonably read the macro content as if it were the wrong strategy's result. Fixed globally in `AppShell.tsx` (a `useEffect` on `location.hash` calling `scrollIntoView`), not per-page, so every current and future hash link benefits. (2) `get_latest_signal_validation_run`'s read-back path was missing `factor_count` (only the live POST-run response had it), rendering as "Not available" on the page. Fixed by deriving it from the distinct factor keys in the stored correlation pairs. (3) User follow-up, correctly identified as a design problem: the page was grouped by strategy ("Macro regime factors" / "Cross-sectional momentum factors" sections), which buries the more important question -- what LEVEL is this result at -- one level below what a viewer sees first. Regrouped to grade granularity-first (Component / Ensemble / Strategy / Desk, same order and labels as the metric catalog), strategy as the secondary label inside each section; explicit placeholder sections for `strategy` and `desk` levels state the real gap (no whole-strategy backtest exists yet) instead of just being absent. Live-verified after restart: `cross_sectional_momentum`'s signal-validation result now correctly reads `factor_count: 4` (was "Not available"). |
| 0.18 | 2026-08-25 | **Research page reorganized; strategy-level backtest gap named honestly.** User-identified, direct: "frontend page so so confusing... our infra is not as my experienced one, looks so mass." Confirmed by actually reading the whole page as one piece rather than section-by-section as it was built: five unrelated things (macro-factor significance, macro diversification, momentum diversification, the 71-metric catalog) were stacked under a page still titled "Factor significance," with no visual separation and no click-through between the Strategy registry and its own research results. `FactorSignificancePage.tsx` renamed to `ResearchPage.tsx` (route unchanged) and restructured into one section per strategy family (macro regime, cross-sectional momentum), each with a real "Registry record" link out; the Strategy detail page gained a matching "View research" header link and a per-run "View full result" link for signal-validation runs, anchored to the correct section (`RESEARCH_ANCHORS`, an explicit small map, not inferred, so a link never points at a section that doesn't exist). Also surfaced, not yet fixed: there is still no whole-strategy backtest for `cross_sectional_momentum` -- nothing takes the composite ranking and actually trades it (rank -> buy top-N -> hold -> rebalance) to produce a real equity curve, unlike the single-name MACD/RSI backtest that already exists. All 40 `strategy`-granularity catalog metrics are honestly dashed because of this real gap, not a UI oversight -- scoped as the explicit next piece of engine work, not attempted in this pass. |
| 0.17 | 2026-08-25 | **Metric granularity axis, user-identified gap.** The 71-entry catalog (0.15) flattened three genuinely different levels into one list -- caught by the user, not found internally: "some are single factor metrics, like IC, then some are higher level metrics like ranking ICs, then overall you have return test metrics... a genetic algorithm can fit." New `research_metric_catalog.granularity` column (`component` \| `ensemble` \| `strategy` \| `desk`), independent from `category` (what kind of metric) -- this is what LEVEL of the strategy hierarchy it evaluates, mapped directly onto the schema that already exists: `component` = one `strategy_components` row alone (IC, decay, turnover); `ensemble` = relationships among a strategy's own components, not yet tradable (correlation, effective number of bets, multiple-hypothesis correction -- exactly what 0.15/0.16 computed); `strategy` = one strategy's own realized backtest (CAGR, Sharpe, PBO -- the tier an optimizer could actually fit); `desk` = cross-strategy portfolio construction. All 71 rows re-tagged (11 component, 6 ensemble, 40 strategy, 14 desk), the cross-cutting categories (data integrity, robustness, trading reality) scattered by what they actually check rather than given a 5th tag, per explicit confirmation. Research page's metric-catalog panel now groups top-to-bottom by level, then by category within each level -- the "top/sub/sub-of-sub" rendering asked for. Live-verified after a full DB reset and re-run: both metrics with real data today (`factor_correlation`, `effective_number_of_bets`) correctly self-report as `ensemble` -- the taxonomy agrees with what was actually built, not asserted independently of it. |
| 0.16 | 2026-08-25 | **First research-loop smoke test, closing the coder/research context-switch gap.** Added one genuine literature-classic candidate factor -- Jegadeesh & Titman's (1993) original "12-1" momentum (12-month return, most recent month skipped) -- to `cross_sectional_momentum`'s signal-validation extraction, registered as a `strategy_components` row (`status='draft'`: real, grey-testable, not yet promoted into the live blend). The point was the diff size, not the factor: ~10 lines in one existing function (`_momentum_horizon_series`), zero schema changes, zero new endpoints, zero UI changes -- the correlation/ENB math, the DB write, and the Research page's existing diversification panel all absorbed it unchanged. Live-proved end to end: DB insert verified directly by SQL, the GET read-path verified separately from the write, the Strategy registry's existing "Ensemble components" panel renders the new draft row with no code changes. Result is real and honest, not just plumbing-proof: 12-1 momentum correlates 0.68 with 6M, 0.44 with 3M, only 0.08 with 1M (consistent with the literature's short-term-reversal-vs-momentum distinction) and is NOT flagged redundant -- effective number of bets rose from 1.74 (3 factors) to 2.11 once included, a genuinely diversifying addition. IC/Rank-IC against forward returns deliberately not run this pass (an honest catalog dash, not a gap disguised as a result) -- proving swap-safety was the goal, not exhausting the taxonomy. |
| 0.15 | 2026-08-25 | **Research-evidence layer, first slice: signal validation / effective number of bets.** Generalizes the narrow factor_significance_runs pattern (macro-factor-vs-symbol only) into a reusable framework for the full quant-research taxonomy the user specified (data integrity, signal validation, backtest performance, robustness/statistical validation, trading reality, portfolio/risk -- 71 catalog entries total). New `research_metric_catalog` enumerates every metric that CAN be computed, tagged by which `strategies.family` values it structurally applies to (e.g. backtest-performance metrics don't apply to `macro_regime` -- a classifier has no trade to have a Sharpe ratio on); new EAV `research_run_metrics` holds only what a real run actually produced -- a catalog entry with no metrics row renders as an honest dash, never an implied oversight, matching "I enumerate all possible metrics... does not mean we always need to run all." `research_runs` extended with `component_key` (which strategy_component a run targets, if not the whole ensemble), `superseded_by_run_id`, and `invalidated_reason` -- schema-ready for correcting a research mistake without deleting sealed history, mirroring the strategy_versions supersession pattern; no endpoint uses these two columns yet, stated honestly rather than silently. New `backend/engine/research/signal_validation.py` (11 tests, pure, standalone): Rank IC, IC-series mean/std/ICIR, pairwise Pearson correlation matrix, effective number of bets (inverse-Herfindahl of PCA eigenvalues -- N for orthogonal factors, ~1 for fully redundant ones), and redundancy flagging (\|r\| >= 0.7). `POST /api/v1/admin/research/signal-validation/runs` wires this to real, point-in-time-aligned extraction for the two factor families that exist today (macro's 8 factors via nearest-prior FRED observation alignment; momentum's 3 horizons via the same pooled-sample convention as momentum_v2.py) -- a third family needs one new extraction function, not new tables or new math. First live run against real data validates the user's own worked example almost exactly: macro's 8 factors -> **effective number of bets = 2.43** (10 of 28 pairs flagged redundant -- CPI/core-PCE/PPI cluster at r=0.92-0.998, all three real inflation measures), momentum's 3 horizons -> ENB = 1.74 (3M/6M redundant at r=0.75, 1M more independent -- consistent with the published short-term-reversal-vs-medium-term-momentum split). Research → Operations page extended with a diversification panel per family (correlation table, color-shaded by \|r\|, run button) and a full metric-catalog reference table (all 71 entries, real-data flag per metric). Only signal_validation has real utilities so far; the other 5 categories are populated as honest, enumerated, un-computed catalog rows -- the "clean, fingerprinted, no accumulating mistakes" and "null allowed" requirements are the delivered infra; backtest-performance/robustness/trading-reality/portfolio-risk math is future work, same pattern, no new plumbing needed. Also fixed, found and fixed inline while proving this live (unrelated to the research layer itself): `fetch_data`'s FRED realtime pin used the sandbox's UTC "today," which can be a full day ahead of FRED's own server clock for several hours after UTC midnight, causing a real, reproducible HTTP 400 ("realtime_start can not be after today's date"); fixed with a 1-day safety margin, since ALFRED vintages don't change intraday anyway. |
| 0.14 | 2026-08-25 | **Methodology thinned; both null desks registered; extensibility boundary documented.** Methodology page rewritten: one card per named desk (top-level parameter + code reference + literature only), the hand-maintained step-by-step formula walkthrough dropped after it went stale mid-milestone (it still named the retired `backtest.py` after `backtest_v2.py` shipped in 0.13) -- deep structural detail now lives only on each desk's Registry record, not duplicated in static text. `sentiment_text_mining` and `fundamental_analysis` registered as honest `strategies` rows (`status='draft'`, `current_version=NULL`, no fabricated implementation) so the full 6-desk shape -- 5 real, 2 explicitly null -- is visible on both the Methodology page and the Strategy registry, not held only in a maintainer's memory. Verified a fresh-clone claim rather than assuming it: a DB built from `schema.sql` alone, zero pipeline run, zero manual SQL, reproduces the exact live registry state (strategy/version/component rows match byte-for-byte). Extensibility boundary clarified on request, deliberately NOT built further (zero-recode dispatcher explicitly declined as over-engineering for this stage): adding/retiring an EXISTING registered component is pure SQL, proven live in 0.13; adding a brand-NEW timing-family signal still needs one small, localized edit to `backtest_v2.py`'s orchestrator (macro's null-tolerant weighted sum has no such limit -- any number of named factors add with zero code change there). A component's `status='watching'` already grey-tests for real (counts toward live computation, flagged for review) -- true parallel shadow-evaluation that doesn't touch production numbers would need generalizing `research_runs`/`factor_significance_runs` beyond its current macro-factor-only scope; not built, not required for what's registered today. |
| 0.13 | 2026-08-25 | **Sub-strategy granularity, demonstrated on `macd_rsi_single_name_timing`**: 0.12 proved a top-level strategy can be swapped safely; this closes the granularity gap one level deeper -- a strategy can itself be an ensemble of named, independently versioned, independently retireable components, not a fused function. New `strategy_components` table (schema.sql) registers each component's type (`computed`: a real function, value comes from the engine each run; `manual_override`: a human-set standing value with no data source, e.g. a normally-neutral geopolitical/war-risk override settable to an extreme like -100, with a full audit trail -- schema supports this from day one though no strategy uses it yet), roles (what an ensemble can use it for), status (the actual retire switch -- a DB flag flip, no code deploy), and its own verification/decay bookkeeping. `macd_rsi_single_name_timing` naive-v2 (`backend/engine/timing/backtest_v2.py`) splits into `macd_crossover` (entry+exit) and `rsi_overbought_exit` (exit only) -- deliberately NOT combined via macro's null-tolerant weighted average, because they are not peers in a sum; they play different roles in a sequential rule (MACD is the only entry trigger). A role-tagged signal ensemble is the correct shape here, not a forced reuse of the weighted-composite pattern. Live-proved against today's real data, not just unit tests: retiring `rsi_overbought_exit` (a real DB update, real pipeline re-run) degrades gracefully -- QQQ's backtest return changed (+118% -> +142%, some exits now fire later) while trade count and every pipeline stage completed unchanged. Retiring `macd_crossover` too produces an explicit `no_entry_signal_active` status with zero trades and a plain-language reason, not a crash or a fabricated entry rule -- and the pipeline still completed all 8 stages. Both components were then restored to active. Components are now surfaced end-to-end: `get_strategy()` returns them, and the Strategy detail page renders an "Ensemble components" panel. |

## What "working" means, per milestone

The same word means different things at different milestones. Read the status line for the milestone you're actually in before deciding what "not done yet" implies — this section exists specifically so a future session doesn't over- or under-react to "make it work."

| Milestone | "Working" means | "Working" does **not** require |
| --- | --- | --- |
| 3 (done) | Every displayed number is the output of a real function reading real fetched data. Naive, hand-picked coefficients, collinear factors, and even overfit or already-public formulas are accepted. No page shows "Not available" for anything the engine is capable of computing. | Statistical significance, decorrelation, decay estimation, or the naive rule beating a simple baseline. It's fine — expected — for the naive backtest to lose to buy-and-hold. |
| 4 (not started, scoped below) | Every factor has passed a real significance test and been decorrelated from its peers before its weight is fit (not hand-picked) through an iterative search. Decay is explicitly defined and measured. | Paid data, production-scale infrastructure, or matching a real fund's actual process — only its statistical rigor. |
| 5 (not started) | Same rigor as Milestone 4, extended once the richer paid-provider dataset (earnings, options chains, economic-calendar expectations) is available. | — |

## Milestone 1 — Local environment checked

Status: **done** (2026-08-24).

- Backend: Python venv with `backend/requirements.txt` installed; `pytest -q` — 107/107 passing.
- Frontend: `npm --prefix frontend run build` succeeds; production bundle serves from the backend.
- App: `python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000` boots; all endpoints serve real, database-backed data.

## Milestone 2 — Staging/production mode toggle

Status: **done** (2026-08-24). Table `engine_operating_mode` (singleton row), `PUT /api/v1/admin/engine-mode`, toggle UI on Operations → Credentials.

A DB-backed, UI-visible toggle on the Operations → Credentials page (not a workspace/decision page) switching the engine between:
- **Pilot mode** (default): free-data-only. Runnable by anyone who clones the repo with zero paid credentials.
- **Production mode**: same engine functions, full paid-provider stack, once built.

Real mechanism, verified: `run_pipeline` blocks any stage requiring a `tier='paid'` provider while in pilot mode, and every snapshot a run produces is stamped with the mode active when it ran.

## Milestone 3 — Real engine on free data, staging mode

Status: **done** (2026-08-24). All six compute stages (`fetch_data`, `validate_data`, `regime_filter`, `factor_engine`, `allocation_engine`, `instrument_engine`) are real and verified end-to-end against live FRED + Yahoo data. Only `publish_snapshot` remains scaffolded.

**`regime_filter`**: 8 real macro factors (growth, inflation, PPI, core PCE, employment, liquidity, volatility, rates), every number traceable to a fetched FRED observation.

**`factor_engine`**: real 10-year price history for all 21 tradeable staging symbols, real cross-sectional momentum ranking (blended 1M/3M/6M z-score), and a real per-symbol MACD(12,26,9)/RSI(14) backtest with a full trade log. Verified example (QQQ, 2016-2026): 100 closed trades. Desk-level aggregate across all 22 backtested symbols (2026-08-24 run): mean strategy return +122% vs. mean buy-and-hold +1558% — the naive rule loses to holding, on average, largely dragged down by a handful of extreme decade-long compounders (e.g. NVDA, BTC-USD) in the buy-and-hold comparison. Expected and fine per this project's standing rule: a working, naive, even losing result beats no result, because it leaves something to optimize.

**`allocation_engine`**: a real risk envelope — regime confidence scales a gross-exposure multiplier, cross-sectional composites roll up into sleeve targets — persisted as a real decision graph (desk → risk envelope → sleeves). Verified example (2026-08-24 run): regime confidence 0.49 → 0.99x multiplier → target gross exposure 98.6% across 6 sleeves.

**`instrument_engine`**: the full -5..+5 conviction scale mapped to concrete instrument expressions (|1.0-2.4| equity tilt, |2.5-3.4| credit spread, |3.5-4.4| debit spread, |4.5-5.0| LEAPS), priced with a real Black-Scholes engine (real spot price, real realized volatility, real 10-year Treasury rate). Verified example: IGV at conviction +5.0 → LEAPS long call, 103.37 strike, 545 DTE, 9 contracts, max loss $18,895.61 (2% of the $1,000,000 staging budget). Every options candidate is honestly labeled theoretical-pricing-only (no free options-chain source exists) and carries a required, unresolved blocker — the existing readiness gates correctly keep these non-executable.

Known simplification, stated not hidden: cross-sectional discovery and single-name
timing currently live in the same `factor_engine` stage, even though this project's
own rule calls for them to stay "separately revisioned and evaluated." Splitting them
into their own pipeline stage is future work.

Working rule for this milestone, stated plainly so it doesn't get re-litigated: **a naive, overfit, collinear, or "already-decayed-and-public" factor formula is acceptable here.** The only non-negotiable is that every output number comes from a real function reading real fetched data — never a hand-typed final value standing in for one. Quality, redundancy, and decay get addressed in Milestone 4, not before.

## Milestone 4 — Optimize within pilot mode

Status: not started. Scoped to the staging universe only, still free-tier data. This is deliberately the first milestone where "naive is fine" stops applying — see the vocabulary table above. Every step below is real, non-trivial quant work; none of it is something one agent does casually in an afternoon, and it is explicitly sequenced so rigor comes before fitting, never after.

**Step order matters — later steps depend on earlier ones passing first:**

1. **Statistical validation.** For every existing factor — both the macro composite's 8 inputs (`engine/regime/`) and the cross-sectional momentum blend's 3 horizons (`engine/factors/`) — run a real significance test (e.g. IC t-stat / p-value against forward returns) before that factor is trusted with any weight at all. A factor that doesn't clear significance gets flagged, not silently kept at its current hand-picked weight.
2. **Decorrelation.** Apply PCA / dimensionality reduction across the factors that do pass step 1, macro and cross-sectional separately at first. The goal is to stop double-counting the same underlying bet (e.g. two momentum horizons that are 90% correlated shouldn't each get independent weight) — this is the project's own governing principle ("a regime modifier is not automatically alpha... double-counts evidence," [edition-v1.md](editions/edition-v1.md)) applied concretely with math instead of asserted in prose.
3. **Decay definition.** For each factor that survives steps 1-2, explicitly measure how its predictive power fades with time (half-life, not an assumption). This is the "real library, real research team" tier of work the user named directly — treated as seriously as a systematic fund would, not faked or skipped to reach a number.
4. **Weight fitting — only after 1-3 pass.** Only once a factor is statistically significant, decorrelated from its peers, and has a measured decay does it become eligible for a fitted (not hand-picked) weight — an iterative search (walk-forward, many epochs, starting from a small weight and converging, e.g. 0.001 → ~0.6) rather than the current typed-in constants (`regime/scoring.py`'s `WEIGHTS`, `factors/momentum.py`'s 0.2/0.3/0.5 blend). Macro composite weights and cross-sectional/momentum weights are fit as separate exercises, matching this project's standing rule that cross-sectional discovery and single-name timing stay independently revisioned.
5. **Ensemble across factor families.** Once the cross-sectional (discovery) and time-series (timing) layers each have their own validated, decorrelated, fitted factors, combine them as an ensemble rather than the current single combined `factor_engine` stage — the known simplification flagged in Milestone 3 gets resolved here, not before.

Address the desk-level backtest aggregate's naive-vs-buy-and-hold gap (Milestone 3's honest, expected result) as a natural output of this process, not a separate task bolted on afterward.

## Milestone 5 — Switch to production mode

Status: not started. Gated on Milestone 4 being good enough to trust the plumbing.

- Register Intrinio / Benzinga / Trading Economics only once their adapters exist.
- Production mode unlocks fields pilot mode structurally cannot have (e.g. earnings — ETFs don't report them).
- Re-run the same optimization pass from Milestone 4 with the richer data available.

## Open decision — `publish_snapshot`

Not yet decided whether `publish_snapshot` needs its own implementation or is redundant: `run_pipeline`'s orchestrator already seals the dataset and desk snapshot directly at the end of a run, regardless of this stage's status. Revisit once Milestone 4 work clarifies whether a distinct publish step earns its keep.
